package com.mobilevault.backend.repository;

import com.mobilevault.backend.entity.SellRequestImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SellRequestImageRepository extends JpaRepository<SellRequestImage, Long> {
}
